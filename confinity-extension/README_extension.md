Confinity Companion extension skeleton

Features included as placeholders:
- Tab Groups helper (uses chrome.tabGroups where supported)
- AI integration placeholders (requires user-provided API key stored in extension storage)
- Simple ad-block pattern file (adblock_list.txt) — integrate with dNR or webRequest per browser
- Brave search quick box (opens search.brave.com)

Next steps:
1. Provide AI API key to enable AI features.
2. Replace adblock_list.txt with curated lists and implement declarativeNetRequest rules.
3. Test and publish to Chrome Web Store and Mozilla Add-ons separately.
